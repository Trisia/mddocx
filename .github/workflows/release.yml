name: Release

on:
  push:
    tags: [ 'v*' ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npmjs.org'

      - name: Install Python dependencies
        run: pip install python-docx Pillow requests mistune

      - name: Smoke test — convert demo
        run: python skills/mddoc/scripts/md2docx.py examples/demo.md -o /tmp/test.docx

      - name: Package skill (.zip)
        run: cd skills && zip -r ../mddoc.skill.zip mddoc/

      - name: Package source
        run: zip -r mddocx.zip . -x ".git/*" "__pycache__/*" ".venv/*"

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish to ClawHub
        run: npx clawhub sync --all --source-repo ${{ github.repository }} --source-commit ${{ github.sha }} --source-ref ${{ github.ref_name }}
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            mddoc.skill.zip
            mddocx.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
